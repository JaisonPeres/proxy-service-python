# Proxy Service

## Enviroments
Copy example of env and configure vars


```sh
cp .env.example .emv
```

## Install with Pip
```sh
pip install -r requirements.txt
```

## Install with Pipenv
```sh
pipenv shell && pipenv install
```

## Running

```sh
python api.py
```
